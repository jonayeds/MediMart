"use client"
import { Button } from "../ui/button"

const MakePayment = () => {
    const handleMakePayment = ()=>{

    }
  return (
    <div className="flex justify-center">

    <Button  onClick={handleMakePayment}  className=" mt-8">Pay Now</Button>
    </div>
  )
}

export default MakePayment